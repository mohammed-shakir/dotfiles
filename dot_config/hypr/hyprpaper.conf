# Preload images into RAM (one per line)
preload = /home/shakir/Pictures/wallpapers/red-moon.png

# Assign wallpapers to outputs (monitor names from `hyprctl monitors`)
wallpaper = eDP-1,/home/shakir/Pictures/wallpapers/red-moon.png

# Optional: render splash text over wallpaper
splash = false

# Disable IPC entirely (turn off periodic polling)
ipc = off
